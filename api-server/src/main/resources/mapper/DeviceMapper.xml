<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iotplatform.mapper.DeviceMapper">

    <select id="findBySiteId" resultType="com.iotplatform.model.Device">
        SELECT device_id, site_id, device_name, device_type, rated_power_kw, is_active, created_at
        FROM dim_devices
        WHERE site_id = #{siteId}
        ORDER BY device_id
    </select>

    <select id="findById" resultType="com.iotplatform.model.Device">
        SELECT device_id, site_id, device_name, device_type, rated_power_kw, is_active, created_at
        FROM dim_devices
        WHERE device_id = #{deviceId}
    </select>

</mapper>
